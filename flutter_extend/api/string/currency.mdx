---
title: "String - Currency & Number Formatting"
sidebarTitle: "Currency & Number Formatting"
icon: "text-height"
description: "Format raw numeric strings into locale-aware, human-readable numbers with thousand separators, leveraging the intl package."
keywords: ['Flutter String', 'String', "String Format", 'Flutter']
---
<Steps titleSize="h3">
    <Step title="🛠️ Formatting">
        {/*  ------------------ */}
        <ResponseField name="thousandNumberFormat" type="String">

        Applies thousand separators to a numeric string based on the default system locale (`intl` required). Handles non-numeric input safely by returning "0".

        ```dart lines wrap
        final amountString = '10000000';
        print(amountString.thousandNumberFormat); // "10,000,000"
        ```
        </ResponseField>

        {/*  ------------------ */}
        <ResponseField name="cleanNumbers" type="String">

        Removes all commas (,) from the string. Essential for sanitizing user input before parsing it as a number.

        ```dart lines wrap
        final messyString = '10,000,000.50';
        print(messyString.cleanNumbers); // "10000000.50"
        ```
        </ResponseField>
    </Step>
</Steps>

### 🛠️ Formatting


